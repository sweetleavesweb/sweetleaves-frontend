WP_GRAPHQL_ENDPOINT=https://cms.sweetleaves.co/graphql
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Age gate
# AGE_GATE_TTL_HOURS=12
# AGE_GATE_COOKIE_NAME=age_gate_verified
# AGE_GATE_SECRET=replace-with-a-long-random-string

# Backward-compatible fallback if AGE_GATE_TTL_HOURS is omitted
# NEXT_PUBLIC_AGE_GATE_TTL_HOURS=12

# Cache revalidation
WP_REVALIDATE_SECONDS=0
WP_COA_REVALIDATE_SECONDS=0

# Optional analytics script
NEXT_PUBLIC_ALPINEIQ_SCRIPT_SRC=
